#set($inputRoot = $input.path('$'))
{
  "user_id" : $input.json('$.user_id'),
  "organization_name" : $input.json('$.organization_name'),
  "organization_description" : $input.json('$.organization_description'),
  "stage_name" : "${stageVariables.stage_name}"
}
